# Local frontend dev env
PORT=4173
BASE_PATH=/

# Leave empty locally so the Vite dev proxy sends /api to http://localhost:3000.
# On Vercel, either omit this to use https://satvikam.onrender.com
# or set it to a different backend origin when needed.
VITE_API_BASE_URL=
